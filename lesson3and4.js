/* если добавить к стрингу +, то он станет интом
это то же, что и Number(let)

можно написать 
console.log(`Текст, а потом '${variable}' значение переменной`)

freeShipping = currentPrice > 1000
значение запишется в переменную только если она 
удовлетворяет условию

литерал - значение, которое ты записываешь в переменную

можно сделать экранирование кавычек
console.log('The message is \'Hello world\'')
вывод: The message is 'Hello world'

Boolean(value) - это по факту проверка на отсутствие 
значений undefined, NaN, 0, " " и null

можно писать как String(value) так и value.toString
*/

